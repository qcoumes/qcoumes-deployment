server {
  listen 80;
  server_name _;

  location /downloads/ {
    alias /var/www/downloads/;
    autoindex on;
    autoindex_localtime on;
    autoindex_exact_size off;
  }

  location / {
    return 302 /downloads/;
  }
}
